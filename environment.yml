name: nxtgm
channels:
  - conda-forge

dependencies:
  # Build tools
  - cmake
  - doctest
  - cxx-compiler
  - ninja
  # dependencies
  - boost-cpp
  - fmt
  - nlohmann_json
  - highs
  - xtensor
  - xplugin >= 0.2.0
  - tsl_ordered_map
  # dependencies pythonbindings
  - python
  - pybind11
  - xtensor-python
  - numpy
  # python testing
  - pytest
  # for examples
  - sphinx-gallery
  - matplotlib
  - scikit-image
  # documentation
  - doxygen
  - sphinx < 7
  - sphinx_rtd_theme
  - breathe
  - sphinx-sitemap
